import React from 'react';
import { Text, View } from 'react-native';

const Saved = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-5xl font-bold text-dark-200">Ariya</Text>
    </View>
  );
};

export default Saved;
